#include <stdio.h>



int main() {
typedef int entero;

entero x = 1; 

printf( "El valor de x es: %d\n", x );

return 0;
}

// Definir una estrucutra de tipo punto 2d que tenga un id y dos numeros de puntos flotantes tipo double que represente x e y (Usar typedef y struct)